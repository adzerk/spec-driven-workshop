### Requirement: Subset runs use the full canonical catalog

#### Scenario: Property coverage can span files [TRACE-SECONDARY]
- **WHEN** a property references the scenario identifier
- **THEN** the identifier is validated even during subset runs
